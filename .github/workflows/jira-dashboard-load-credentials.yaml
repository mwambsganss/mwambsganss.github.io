name: Jira Dashboard — Load ECR Credentials

on:
  workflow_dispatch:
  pull_request:
    paths:
      - 'Jira Dashboard/**'
  push:
    branches: [master]
    paths:
      - 'Jira Dashboard/**'
  create:  # tag creation

jobs:
  load-credentials:
    uses: EliLillyCo/hangar/.github/workflows/load-credentials.yaml@main
    secrets: inherit
